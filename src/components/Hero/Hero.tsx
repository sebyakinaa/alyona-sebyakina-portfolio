import {useLayoutEffect,useRef} from 'react';import {heroObjects} from '../../data/heroObjects';import {HeroObject} from './HeroObject';import {createHeroMotion} from './heroMotion';import './Hero.css';import './HeroAmbient.css';import './BadgeCleanup.css';export function Hero({reduced}:{reduced:boolean}){const ref=useRef<HTMLElement>(null);useLayoutEffect(()=>ref.current?createHeroMotion(ref.current,reduced):undefined,[reduced]);return <section ref={ref} id="top" className="hero" aria-label="Интро"><div className="hero__stage"><div className="hero-composition">{heroObjects.map(i=><HeroObject key={i.id} item={i}/>)}</div></div></section>}
