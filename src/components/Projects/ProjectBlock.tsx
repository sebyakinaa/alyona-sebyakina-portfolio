import {useLayoutEffect,useRef} from 'react';import type {Project} from '../../data/projects';import {ExternalLink} from '../ExternalLink/ExternalLink';import {ProjectGallery} from './ProjectGallery';import {createProjectMotion} from './projectMotion';export function ProjectBlock({project,reduced}:{project:Project;reduced:boolean}){const ref=useRef<HTMLElement>(null);useLayoutEffect(()=>ref.current?createProjectMotion(ref.current,reduced):undefined,[reduced]);return <article ref={ref} id={project.id} className="project" data-project={project.id}><div className="project__copy"><h3>{project.title}</h3><p>{project.description}</p><ExternalLink href={project.caseUrl}>Смотреть проект</ExternalLink></div><ProjectGallery project={project}/></article>}
