import type {Project} from '../../data/projects';import './Projects.css';export function ProjectGallery({project}:{project:Project}){return <div className="gallery">{project.images.map((im,i)=><figure key={`${project.id}-${i}`} className={`gallery__card gallery__card--${im.area}`}>{im.src?<img src={im.src} alt={im.alt} width={i===0?358:i===1||i===2?580:i===5?858:484} height={i===0?542:i===1||i===4?270:i===2?509:i===3?256:237} loading="lazy" decoding="async" data-parallax={im.parallax??undefined}/>:<span className="gallery__placeholder" aria-label={`${im.alt} — изображение ожидается`}/>}</figure>)}</div>}
