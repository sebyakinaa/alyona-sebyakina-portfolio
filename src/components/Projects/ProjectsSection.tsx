import {projects} from '../../data/projects';import {ProjectBlock} from './ProjectBlock';import './Projects.css';export function ProjectsSection({reduced}:{reduced:boolean}){return <section id="projects" className="projects" aria-labelledby="projects-heading"><h2 id="projects-heading">Проекты</h2><div className="projects__list">{projects.map(p=><ProjectBlock key={p.id} project={p} reduced={reduced}/>)}</div></section>}
