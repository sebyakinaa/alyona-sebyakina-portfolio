import {describe,expect,it} from 'vitest';import {getProjectById,projects} from '../../src/data/projects';describe('project data',()=>{it('contains three six-image projects',()=>{expect(projects).toHaveLength(3);expect(new Set(projects.map(p=>p.id)).size).toBe(3);projects.forEach(p=>expect(p.images).toHaveLength(6))});it('finds project',()=>expect(getProjectById('sleep')?.title).toBe('Sleep'));it('uses real secure case URLs',()=>projects.forEach(p=>expect(p.caseUrl).toMatch(/^https:\/\//)))});
